VITE_USE_MOCK=false

# Use relative URL in dev so Vite proxies /api → localhost:3000 (required for httpOnly cookies)
VITE_API_BASE_URL=/api/v1

# External QA Automation app URL (optional)
VITE_QA_AUTOMATION_URL=
